services:
  task-api-pagination-csharp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: task-api-pagination-csharp
    ports:
      - "3004:3000"
    volumes:
      - .:/workspaces/feature-4-pagination
      - dotnet_packages:/root/.nuget/packages
    working_dir: /workspaces/feature-4-pagination
    command: dotnet run --urls http://0.0.0.0:3000
    restart: unless-stopped

volumes:
  dotnet_packages:

networks:
  default:
    name: restws-network
